# 최신 콘텐츠 스냅샷 및 사건 실행 검증

- 원본: `1mpixlPo_5ccd6-Z5bdb5pxfAMND7_C9i1crgALx3R7U`, 원신 CRPG. 원본은 읽기만 수행했다.
- 메타데이터에서 확인한 61개 탭을 정확한 이름과 헤더로 읽었다. 헤더/기본키 범위를 확인한 뒤 100~160행의 제한된 범위로 계산된 값을 수집했다. 개인 원고의 빈 행도 원래 행번호로 보존했다. `00_CORE` 기본키 범위 뒤에 남은 레거시 8행도 별도로 확인했다.
- 시작·끝·추가 검증 시 원본 수정 시각은 모두 `2026-09-24T11:22:08.295Z`다.
- `db_latest.json`: 기존 실행기의 `tab → [header, ...rows]` 형식과 호환되는 원시 계산값 스냅샷.
- `normalized-snapshot.json`: 헤더별 객체 및 원본 행번호. 문자열 FALSE를 참값으로 강제 변환하지 않았다.
- `raw-ranges.json`, `metadata-start.json`, `metadata-end.json`, `metadata-final.json`: 읽기 근거.
- `content-manifest.json`: 원본 ID, 수정시각, 탭별 해시, 전체 파일 해시.

## 구조 검증

`validate_snapshot.py`는 계산 오류, JSON 형식, 주요 기본키, 노드 다음 참조·선택그룹·맵·단일 자산·개인 임무·호감 사건·레시피 재료·조우 구성·보스 단계 등을 검사한다. 실제 참조 14,806건과 JSON 826건에서 누락 참조·JSON 오류·수식 오류가 발견되지 않았다. 예시 `ECARD_SAMPLE`은 실제 적 카드 참조 검사에서 제외했다. 참고용 작업계획 `40_CRPG_MIGRATION_PLAN`의 PHASE_ID `4A`는 8·9행에서 중복되며, 실행 ID 중복과 구분해 보고했다.

최신 데이터에는 본편 3,218행(활성 3,088행), 전설임무 24행, 개인장면 비공백 3,207행, 호감 사건 148행이 있다. H01/H02/H03/H04/H05는 각각 24개이며, 점수 최소조건은 20/40/60/80/100으로 모두 일치한다. 일부 상위 사건의 하트 6 조건 56곳은 원시 자료에 잔존하며, 사용자 확정 규격인 최대 하트 5와 일반 점수 확장에 맞춰 실행 어댑터에서 처리해야 한다. 원본 장면·문장은 변경하지 않았다.

## 사건 실행 모듈

`source/runtime_events.js`는 `runtime_nodes.js` 다음에 로드한다. 정확한 루트·현재 노드·분기·임무 상태·선행 영수증을 확인하고 최신 51번 탭의 구조화 사건을 처리한다. 알 수 없는 명령·종류·필드·미지원 수치 변경은 오류로 정지한다.

- 명시 이동·경과 시간, 비거래 물건 보관, 동료 이탈과 영입, 임무 대기/재개, 구슬 한 개의 소유를 처리한다.
- M05의 실제 전투 영수증 인계, 분기별 세계 사실과 주인공 지식, 하프 반환, 12,000모라 배달 정산, 필수 사건 영수증을 갖춘 몬드 종결을 처리한다.
- 같은 사건 재요청은 일수·시간·아이템·모라를 다시 지급하지 않는다. 실패는 기존 거래 실행기를 통해 전체 취소된다.
- 개인 교류 활동은 관계 모듈의 `dailyRelationshipActivity`가 담당한다. 사건 모듈은 별도 일일 보상 경로를 만들지 않는다.
- `mainStoryEntries()` / `MAIN_STORY_ACCEPT {quest:"Q_ISK_MOND_02"}`로 이세계인 2장을 명시 수락한다. 대기 지점은 `STORY_RESUME`로 재개하며, 원래 커서·맵·SAVE에 결합된 재개 영수증을 남긴다.
- `storyDisplayText(row)`는 AA 갈래에 실제로 집필된 첫 인사/재회 인사만 선택한다. 진입 때 저장한 최초 접촉 스냅샷과 원고 메타데이터를 검증한다.

## 명시적 콘텐츠 연결 보완

원본 51번 탭의 `EVT_ISK_M05_AA_TRAVEL_01`는 `MAP_STORMTERROR_LAIR → MAP_MOND_CITY`, 180분 귀환을 명시하지만 원본 47번 탭에는 폐허에 접하는 이동 간선이 없다. 동일한 귀환은 `EVT_ISK_M04_K_TRAVEL_04`에도 존재한다.

실행 모듈의 `PATCH_ISK_M05_AA_RETURN_20260924`는 이 정확한 사건·출발지·목적지·180분 조합만 지원하는 스토리 전용 연결이다. 자유 이동이나 다른 사건의 경로 검사를 해제하지 않는다. 적용 여부를 사건 영수증의 `contentPatch`에 남긴다. 원본 시트에는 쓰지 않았다.

## 시험 범위

- `event-tests/test_events.js`: 19개 확인 통과. 시간 중복 방지, 보관물 비인벤토리 처리, 다른 분기 차단, 출발지 오류 전체 취소, 동료 이탈 시 소유·성장 보존, 구슬 중복 방지, 배달 보상 일회 지급 및 소비 후 재지급 방지, 종결 영수증 누락 차단.
- `tests/test_story_isekai.js`: 새 게임부터 K 472 / AA 513 / AB 582 / B 425단계를 실제 선택·대사 경로로 진행하여 각 작성된 전투 준비 지점 도달. 자동 승리 없음.
- `event-tests/test_postcombat_fixture.js`: 전투 시점에 **합성 전투 영수증을 명시적으로 주입하는 단위시험**이다. K 634 / AA 813 / AB 811 / B 650단계를 거쳐 각 M05 최종 노드·본편 완료 처리 통과. 이 시험은 전투 승리 또는 전투 밸런스를 검증하지 않는다.

이 보고서는 구조와 사건 상태 전이를 검증한다. 모든 전투, 이미지 실내용, 모든 UI, 모든 선택 조합의 합격을 의미하지 않는다.
